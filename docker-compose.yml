services:
  web-rsync:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - MAX_CONCURRENT_JOBS=3
    restart: unless-stopped
