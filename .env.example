DATA_DIR=./data
MAX_CONCURRENT_JOBS=3

# CORS origins (JSON array) — only needed for cross-origin dev (Vue dev server at :5173).
# In production the frontend is same-origin as the API, so CORS is not needed.
# CORS_ORIGINS=["http://localhost:5173"]
